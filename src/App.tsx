import { TokenPreview } from './dev/TokenPreview';

function App() {
  // TEMPORARY — renders the baseline design token preview page.
  // This will be replaced with real layout shells and routing in a later milestone.
  return <TokenPreview />;
}

export default App;
